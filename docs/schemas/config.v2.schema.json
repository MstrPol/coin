{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coin.local/schema/config.v2.schema.json",
  "title": "Coin product config v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["coin", "project", "jenkins"],
  "properties": {
    "coin": {
      "type": "object",
      "additionalProperties": false,
      "required": ["goldenPath", "version"],
      "properties": {
        "goldenPath": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Golden path name, e.g. go-app"
        },
        "version": {
          "type": "string",
          "pattern": "^([=~^\\*]|\\d)[0-9A-Za-z.+\\-*]*$",
          "description": "Semver GP pin: =1.0.0 (exact), ~1.0.0 (patch), ^1.0.0 (minor), * (latest stable)"
        },
        "resolve": {
          "type": "string",
          "enum": ["remote", "file"],
          "default": "remote",
          "description": "Manifest source: remote (coin-api→Nexus) or file (local fixture)"
        },
        "manifestFile": {
          "type": "string",
          "minLength": 1,
          "description": "Path to local resolved manifest when resolve=file (default .coin/manifest.local.yaml)"
        }
      }
    },
    "project": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "groupId", "artifactId"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "artifactId": {
          "type": "string",
          "minLength": 1
        },
        "groupId": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "jenkins": {
      "type": "object",
      "additionalProperties": false,
      "required": ["credentials"],
      "properties": {
        "stack": {
          "type": "string"
        },
        "runtime": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "credentials": {
          "type": "object",
          "additionalProperties": false,
          "required": ["docker"],
          "properties": {
            "docker": {
              "type": "string",
              "minLength": 1
            },
            "qgm": {
              "type": "string"
            },
            "nexus": {
              "type": "string"
            }
          }
        }
      }
    }
  },
  "$defs": {
    "stage": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "preCommands": {
          "type": "array",
          "items": { "type": "string" }
        },
        "commands": {
          "type": "array",
          "items": { "type": "string" }
        },
        "postCommands": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    }
  }
}
