# Build stacks → GP embedded pipeline

> **Superseded (2026-07):** отдельный platform component `gp-content` и UI `/platform/build-stacks` **удалены** как SoT.  
> Pipeline-inline редактируется на **GP release** (`/gp/{name}/releases/...`).  
> См. [adr/gp-embedded-pipeline.md](../adr/gp-embedded-pipeline.md), [how-to/publish-gp-release.md](publish-gp-release.md), [golden-paths.md](../golden-paths.md).

Историческая модель Build Stack / Component Studio не является live path. Seed defaults: `coin-api/internal/gpcontent/seed/pipelines/`.

Layout репозиториев: [workspace-layout.md](../workspace-layout.md).
