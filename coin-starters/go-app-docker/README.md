# go-app-docker starter

BYO Dockerfile sample for Golden Path `go-app-docker` (`build.engine: dockerfile`).

Product repo must contain `Dockerfile` with `test` and `runtime` targets matching the GP stack policy.
