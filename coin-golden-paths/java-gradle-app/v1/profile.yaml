# CI agent: coin-jenkins-agents/stacks/java-gradle/17
agent:
  stack: java-gradle
  runtime:
    java: "17"
build:
  type: container
  dockerfile: Dockerfile
publish:
  kind: registry
  when: tag
pipeline:
  test:
    enabled: true
  build:
    enabled: true
  publish:
    enabled: true
