schemaVersion: 2
name: semver-tag

branches:
  - name: main
    pattern: ^main$|^master$
    versioning:
      template: "v{base}"
    publish: true

  - name: feature
    pattern: ^feature/.+$
    versioning:
      template: "v{base}"
    publish: false
