jobs:
  - script: >
      pipelineJob('agents-build') {
        description('Platform: CI agent images → Nexus Docker (coin-jenkins-agents)')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('http://gitea:3000/coin/coin-jenkins-agents.git')
                  credentials('gitea-git')
                }
                branch('main')
              }
            }
            scriptPath('Jenkinsfile')
          }
        }
      }
