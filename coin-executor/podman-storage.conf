# Podman storage in k8s agent pod: graphroot on emptyDir (see coin-pod-template.yaml).
[storage]
driver = "overlay"

[storage.options]
# Allow running in unprivileged k8s pods (single UID mapping)
ignore_chown_errors = "true"
