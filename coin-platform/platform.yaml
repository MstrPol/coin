# Platform-wide infra defaults (coin-lib). Версии CLI и agent для продуктов — в GP profile.yaml.
jenkins:
  jnlp:
    image: jenkins/inbound-agent:3327.v868139a_d00e0-8

nexus:
  mavenBase: http://nexus:8081/repository
  credentialsId: nexus-admin

# Глобальный пол совместимости CLI; продукт управляется только templateVersion GP.
coinCli:
  min: "0.0.0"
